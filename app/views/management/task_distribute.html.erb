<%= javascript_include_tag 'task.js' %>
<%= stylesheet_link_tag 'management'%>
<h1><%= t('task_distribute') %></h1>
<div class="addTask">
  <form action="addTask" method="post" id="addProjectForm">
    <%= t('task_name') %>:<input name="name" placeholder="<%= t('input_task') %>"/>
    <%= t('project_name') %>:
    <select name="project_id">
      <% @projects.each do |value| %>
          <option value="<%= value.id %>"><%= value.name %></option>
      <% end %>
    </select>
    <%= t('start_date') %>:<input type="date" name="start_date"/>
    <%= t('weight') %>:<input name="weight" placeholder="<%= t('input_weight') %>"/><br>
    <br>
    <%= t('description') %>:
    <textarea rows="3" cols="20" name="description"></textarea>
    <br>
    <br>
    <br>
    <%= t('add_people') %>:
    <br>
    <div class="choosePeople">
      <div class="leftPeople" ondrop="drop(event)" ondragover="allowDrop(event)">
        <div class="headerDiv"><%= t('project_people') %></div>
        <% i = 0 %>s
        <% @people.each do |value| %>
            <div class="data" draggable="true" ondragstart="drag(event)" id="l<%= i %>" data-id="<%= value.id %>"><%= value.name %></div>
            <% i+=1 %>
        <% end %>
      </div>
      <div class="centerChoose">
        --->
      </div>
      <div class="rightPeople" id="rightPeople" ondrop="drop(event)" ondragover="allowDrop(event)">
        <div class="headerDiv"><%= t('task_people') %></div>
      </div>
    </div>
    <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>"/>
    <input type="submit" value="<%= t('add task') %>">
  </form>
  <div id="result" class="hide message"></div>
</div>
